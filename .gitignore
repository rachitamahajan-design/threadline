# Secrets — never commit
.env
.env.*
!.env.example

# Local meeting data (local-first: this never leaves the machine)
data/
*.db
*.sqlite
*.sqlite3
recordings/
*.wav
*.pcm
!samples/**/*.wav

# Dependencies / build
node_modules/
dist/
build/
.next/
__pycache__/
*.pyc
.venv/
venv/

# macOS / editors
.DS_Store
.vscode/
.idea/
*.swp

# internal notes
Untitled
data/google-token.json

# Local scratch / diagnostics (never ship)
capture/tcc-doctor
capture/tcc-doctor.swift
set-pyai-key.sh
*-probe.ts
calibrate*.mjs
embedtest.mjs

# Meeting exports: sharing is an explicit act — never auto-committed
shared-dataset/
